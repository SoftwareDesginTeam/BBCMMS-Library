public enum ItemType{//This will be all the types of items that will be available for loan, i.e book, movie.etc
}
public enum ItemStatus(//This will be all the states the book will be in, available, loaned, reserved, etc.
}
public enum ReservationStatus{//
}
public enum AccountStatus{//Is the account acitve, canceled, blacklisted,etc?
}

public class Address{//(private)parts of address street, city, state, zip.
}
public class Person{//(private)Name, addreess, email or phone? Allowed to text them?
}
public class Constants{//How many books should a user get? How long can they have a book before it is overdue?
}
